/**
 * PolicyIntegrations — Integrations view for Policy Deployment service (admin only).
 */
import IntegrationDashboard from "@/components/IntegrationDashboard";

export default function PolicyIntegrations() {
  return <IntegrationDashboard />;
}
