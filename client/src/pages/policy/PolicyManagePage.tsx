/**
 * PolicyManagePage — Manage Policy view for Policy Deployment service (admin only).
 */
import PolicyManage from "@/components/PolicyManage";

export default function PolicyManagePage() {
  return <PolicyManage />;
}
