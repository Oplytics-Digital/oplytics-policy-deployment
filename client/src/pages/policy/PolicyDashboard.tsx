/**
 * PolicyDashboard — Default landing page for Policy Deployment service.
 * Renders the Dashboard component from the existing Policy Deployment tool.
 */
import Dashboard from "@/components/Dashboard";

export default function PolicyDashboard() {
  return <Dashboard />;
}
