/**
 * PolicyDeployments — Deployments view for Policy Deployment service.
 */
import Deployments from "@/components/Deployments";

export default function PolicyDeployments() {
  return <Deployments />;
}
